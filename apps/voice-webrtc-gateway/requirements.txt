# Nova voice WebRTC gateway: bridges mic audio ↔ agent-core (STT / chat / TTS).
# Requires Python 3.11+. On Windows, use a venv; PyAV wheels are published for win_amd64.
fastapi>=0.115.6
uvicorn[standard]>=0.32.1
httpx>=0.28.1
aiortc>=1.9.0
numpy>=1.26.0
av>=12.0.0

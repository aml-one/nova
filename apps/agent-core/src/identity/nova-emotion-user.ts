/**
 * Nova’s single mood across web, WhatsApp, Signal, every user, plus learning-loop events (SQLite key).
 * Legacy per-contact emotion rows are obsolete for live mood; use this id only for emotion_state / emotion_events.
 */
export const NOVA_PRIMARY_EMOTION_USER_ID = "local-web-user";
